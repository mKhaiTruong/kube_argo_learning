apiVersion: v1
kind: Service
metadata:
  name: grade-submission-api
  namespace: grade-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: grade-submission-api
  ports:
    - port: 8443
      targetPort: https
